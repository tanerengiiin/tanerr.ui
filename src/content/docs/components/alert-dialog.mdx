---
title: Alert Dialog
description: A modal dialog for important actions requiring confirmation.
links:
  doc: https://base-ui.com/react/components/alert-dialog
  api: https://base-ui.com/react/components/alert-dialog#api-reference
---

<ComponentViewer name="alert-dialog-demo"/>

## Installation

<Stepper totalSteps={3}>
  <Step stepNumber={1}>
    <h6>Create `alert-dialog.tsx` file in your `components/ui/` directory.</h6>
  </Step>
  <Step stepNumber={2}>
    <h6>Copy and paste the following code into the file.</h6>
    <ComponentSource name="alert-dialog" />
  </Step>
  <Step stepNumber={3}>
    <h6>Update the import paths according to your project setup.</h6>
  </Step>
</Stepper>

## Usage

```tsx
import {
    AlertDialog,
    AlertDialogTrigger,
    AlertDialogPopup,
    AlertDialogTitle,
    AlertDialogDescription,
    AlertDialogClose,
} from "@/components/ui/alert-dialog";
```

```tsx
<AlertDialog>
  <AlertDialogTrigger render={<Button>Open</Button>} />
  <AlertDialogPopup>
    <AlertDialogTitle>Edit profile</AlertDialogTitle>
    <AlertDialogDescription>
      Make changes to your profile here. Click save when you're done.
    </AlertDialogDescription>
    <div className="flex items-center justify-end gap-2 w-full">
      <AlertDialogClose render={<Button variant="outline">Cancel</Button>} />
      <AlertDialogClose render={<Button>Continue</Button>} />
    </div>
  </AlertDialogPopup>
</AlertDialog>
```

## Examples

### Nested alert dialogs

<ComponentViewer className="mt-6" name="alert-nested-dialog-demo"/>
