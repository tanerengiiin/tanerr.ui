---
title: Scroll Area
description: A component that allows for scrolling content within a defined area.
links:
  doc: https://base-ui.com/react/components/scroll-area
  api: https://base-ui.com/react/components/scroll-area#api-reference
---

<ComponentViewer name="scroll-area-demo" />

## Installation

<Stepper totalSteps={3}>
  <Step stepNumber={1}>
    <h6>Create `scroll-area.tsx` file in your `components/ui/` directory.</h6>
  </Step>
  <Step stepNumber={2}>
    <h6>Copy and paste the following code into the file.</h6>
    <ComponentSource name="scroll-area" />
  </Step>
  <Step stepNumber={3}>
    <h6>Update the import paths according to your project setup.</h6>
  </Step>
</Stepper>

## Usage

```tsx
import { ScrollArea } from "@/store/default/ui/scroll-area";
```

```tsx
<ScrollArea>

</ScrollArea>
```
