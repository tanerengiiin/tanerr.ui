---
title: Badge
description: Compact visual indicator for status and labels.
---

<ComponentViewer name="badge-demo"/>

## Installation

<Stepper totalSteps={3}>
  <Step stepNumber={1}>
    <h6>Create `badge.tsx` file in your `components/ui/` directory.</h6>
  </Step>
  <Step stepNumber={2}>
    <h6>Copy and paste the following code into the file.</h6>
    <ComponentSource name="badge" />
  </Step>
  <Step stepNumber={3}>
    <h6>Update the import paths according to your project setup.</h6>
  </Step>
</Stepper>

## Usage

```tsx
import { Badge } from "@/components/ui/badge";
```

```tsx
<Badge>Badge</Badge>
```

## Examples

### Solid

<ComponentViewer className="mt-6" name="badge-solid-demo"/>

### Ghost

<ComponentViewer className="mt-6" name="badge-ghost-demo"/>


### Outline

<ComponentViewer className="mt-6" name="badge-outline-demo"/>

### Rounded

<ComponentViewer className="mt-6" name="badge-rounded-demo"/>

### Make rounded default

Go to `badge.tsx` and set `rounded` to `true` by default.

```tsx /rounded = true/
function Badge({ className, ..., ..., rounded = true, ...props }: BadgeProps) {
  return (
    <div
    // ...
```