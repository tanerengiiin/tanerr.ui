--- 
title: Switch
description: A component that allows users to toggle between two states.
links:
  doc: https://base-ui.com/react/components/switch
  api: https://base-ui.com/react/components/switch#api-reference
---

<ComponentViewer name="switch-demo" />

## Installation

<Stepper totalSteps={3}>
  <Step stepNumber={1}>
    <h6>Create `switch.tsx` file in your `components/ui/` directory.</h6>
  </Step>
  <Step stepNumber={2}>
    <h6>Copy and paste the following code into the file.</h6>
    <ComponentSource name="switch" />
  </Step>
  <Step stepNumber={3}>
    <h6>Update the import paths according to your project setup.</h6>
  </Step>
</Stepper>

## Usage

```tsx
import { Switch } from "@/components/ui/switch";
```

```tsx
<Switch />
```

## Examples

### Using size prop

<ComponentViewer className="mt-6" name="switch-size-demo" />

### Using tone prop
<ComponentViewer className="mt-6"name="switch-tone-demo" />

### With label

<ComponentViewer className="mt-6" name="switch-label-demo" />
