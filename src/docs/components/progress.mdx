import ComponentViewer from "@/components/docs/ComponentViewer";
import { Button } from "@/components/ui/button";
import CodeViewer from "@/components/docs/CodeViewer";
import { Step, Stepper } from "@/components/docs/Stepper";
import ProgressDemo from "@/components/demo/progress/ProgressDemo";
import ProgressToneDemo from "@/components/demo/progress/ProgressToneDemo";
export const metadata = {
  title: "Progress",
  breadcrumb: ["Docs", "Components", "Progress"],
};

<ComponentViewer json="/data/components/demo/progress/ProgressDemo.json">
  <ProgressDemo />
</ComponentViewer>

## Installation

<Stepper totalSteps={3}>
  <Step stepNumber={1}>
    <h6>Create `progress.tsx` file in your `components/ui/` directory.</h6>
  </Step>
  <Step stepNumber={2}>
    <h6>Copy and paste the following code into the file.</h6>
    <CodeViewer className="mt-3" path="./src/components/ui/progress.tsx" />
  </Step>
  <Step stepNumber={3}>
    <h6>Update the import paths according to your project setup.</h6>
  </Step>
</Stepper>

## Usage

<CodeViewer
  className="mt-3"
  code={`import { 
    Dialog, 
    DialogTrigger, 
    DialogPopup, 
    DialogTitle, 
    DialogDescription 
} from "@/components/ui/dialog";`}
/>
<CodeViewer
  className="mt-3"
  code={`<Dialog>
    <DialogTrigger render={<Button>Open</Button>}/>
    <DialogPopup>
      <DialogTitle>Edit profile</DialogTitle>
      <DialogDescription>Make changes to your profile here. Click save when you're done.</DialogDescription>
    </DialogPopup>
  </Dialog>`}
/>

## Examples

### Using tone prop

You can use linear prop to make the progress bar animate in a linear way.

<ComponentViewer className="mt-3" json="/data/components/demo/progress/ProgressToneDemo.json">
  <ProgressToneDemo />
</ComponentViewer>