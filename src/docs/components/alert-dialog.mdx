import ComponentViewer from "@/components/docs/ComponentViewer";
import { Button } from "@/components/ui/button";
import CodeViewer from "@/components/docs/CodeViewer";
import { Step, Stepper } from "@/components/docs/Stepper";
import AlertDialogDemo from "@/components/demo/alert-dialog/AlertDialogDemo.tsx";
import NestedAlertDialogsDemo from "@/components/demo/alert-dialog/NestedAlertDialogsDemo.tsx";
export const metadata = {
  title: "Alert Dialog",
  breadcrumb: ["Docs", "Components", "Alert Dialog"],
};

<ComponentViewer json="/data/components/demo/alert-dialog/AlertDialogDemo.json">
  <AlertDialogDemo />
</ComponentViewer>

## Installation

<Stepper totalSteps={3}>
  <Step stepNumber={1}>
    <h6>Create <code>alert-dialog.tsx</code> file in your <code>components/ui/</code> directory.</h6>
  </Step>
  <Step stepNumber={2}>
    <h6>Copy and paste the following code into the file.</h6>
    <CodeViewer className="mt-3" path="./src/components/ui/alert-dialog.tsx" />
  </Step>
  <Step stepNumber={3}>
    <h6>Update the import paths according to your project setup.</h6>
  </Step>
</Stepper>

## Usage

<CodeViewer
  className="mt-3"
  code={`import {
    AlertDialog,
    AlertDialogTrigger,
    AlertDialogPopup,
    AlertDialogTitle,
    AlertDialogDescription,
    AlertDialogClose,
} from "@/components/ui/alert-dialog";`}
/>
<CodeViewer
  className="mt-3"
  code={`<AlertDialog>
    <AlertDialogTrigger render={<Button>Open</Button>} />
    <AlertDialogPopup>
      <AlertDialogTitle>Edit profile</AlertDialogTitle>
      <AlertDialogDescription>
        Make changes to your profile here. Click save when you're done.
      </AlertDialogDescription>
      <div className="flex items-center justify-end gap-2 w-full">
        <AlertDialogClose render={<Button variant="outline">Cancel</Button>} />
        <AlertDialogClose render={<Button>Continue</Button>} />
      </div>
    </AlertDialogPopup>
  </AlertDialog>`}
/>

## Examples

### Nested alert dialogs

<ComponentViewer
  className="mt-6"
  json="/data/components/demo/alert-dialog/NestedAlertDialogsDemo.json"
>
  <NestedAlertDialogsDemo />
</ComponentViewer>
