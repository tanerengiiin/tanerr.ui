import ComponentViewer from "@/components/docs/ComponentViewer";
import { Button } from "@/components/ui/button";
import CodeViewer from "@/components/docs/CodeViewer";
import { Step, Stepper } from "@/components/docs/Stepper";
import PopoverDemo from "@/components/demo/popover/PopoverDemo";

export const metadata = {
  title: "Popover",
  breadcrumb: ["Docs", "Popover"],
};

<ComponentViewer json="/data/components/demo/popover/PopoverDemo.json">
  <PopoverDemo />
</ComponentViewer>

## Installation

<Stepper totalSteps={2}>
  <Step stepNumber={1}>
    <h6>Install dependencies</h6>
    <CodeViewer className="mt-3" code={`pnpm add @radix-ui/react-slot`} />
  </Step>
  <Step stepNumber={2}>
    <h6>Copy and paste the code below</h6>
    <CodeViewer className="mt-3" path="./src/components/ui/dialog.tsx" />
  </Step>
</Stepper>

## Usage

<CodeViewer
  className="mt-3"
  code={`import { 
    Dialog, 
    DialogTrigger, 
    DialogPopup, 
    DialogTitle, 
    DialogDescription 
} from "@/components/ui/dialog";`}
/>
<CodeViewer
  className="mt-3"
  code={`<Dialog>
    <DialogTrigger render={<Button>Open</Button>}/>
    <DialogPopup>
      <DialogTitle>Edit profile</DialogTitle>
      <DialogDescription>Make changes to your profile here. Click save when you're done.</DialogDescription>
    </DialogPopup>
  </Dialog>`}
/>

## Examples
