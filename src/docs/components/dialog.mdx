import ComponentViewer from "@/components/docs/ComponentViewer";
import { Button } from "@/components/ui/button";
import CodeViewer from "@/components/docs/CodeViewer";
import { Step, Stepper } from "@/components/docs/Stepper";
import DialogDemo from "@/components/demo/dialog/DialogDemo";
import NestedDialogsDemo from "@/components/demo/dialog/NestedDialogsDemo";

export const metadata = {
  title: "Dialog",
  breadcrumb: ["Docs", "Components", "Dialog"],
};

<ComponentViewer json="/data/components/demo/dialog/DialogDemo.json">
  <DialogDemo />
</ComponentViewer>

## Installation

<Stepper totalSteps={3}>
  <Step stepNumber={1}>
    <h6>Create `dialog.tsx` file in your `components/ui/` directory.</h6>
  </Step>
  <Step stepNumber={2}>
    <h6>Copy and paste the following code into the file.</h6>
    <CodeViewer
      className="mt-3"
      path="./src/components/ui/dialog.tsx"
    />
  </Step>
  <Step stepNumber={3}>
    <h6>Update the import paths according to your project setup.</h6>
  </Step>
</Stepper>

## Usage

<CodeViewer
  className="mt-3"
  code={`import { 
    Dialog, 
    DialogTrigger, 
    DialogPopup, 
    DialogTitle, 
    DialogDescription,
    DialogClose
} from "@/components/ui/dialog";`}
/>
<CodeViewer
  className="mt-3"
  code={`<Dialog>
    <DialogTrigger render={<Button>Open</Button>}/>
    <DialogPopup>
      <DialogTitle>Edit profile</DialogTitle>
      <DialogDescription>
        Make changes to your profile here.
      </DialogDescription>
      <DialogClose render={<Button>Close</Button>}/>
    </DialogPopup>
  </Dialog>`}
/>

## Examples

### Nested dialogs

<ComponentViewer json="/data/components/demo/dialog/NestedDialogsDemo.json">
  <NestedDialogsDemo />
</ComponentViewer>
