import ComponentViewer from "@/components/docs/ComponentViewer";
import { Button } from "@/components/ui/button";
import CodeViewer from "@/components/docs/CodeViewer";
import { Step, Stepper } from "@/components/docs/Stepper";
import RadioGroupDemo from "@/components/demo/radio-group/RadioGroupDemo";

export const metadata = {
  title: "Radio Group",
  breadcrumb: ["Docs", "Components", "Radio Group"],
};

<ComponentViewer json="/data/components/demo/radio-group/RadioGroupDemo.json">
  <RadioGroupDemo />
</ComponentViewer>

## Installation

<Stepper totalSteps={3}>
  <Step stepNumber={1}>
    <h6>Create `radio-group.tsx` file in your `components/ui/` directory.</h6>
  </Step>
  <Step stepNumber={2}>
    <h6>Copy and paste the following code into the file.</h6>
    <CodeViewer className="mt-3" path="./src/components/ui/radio-group.tsx" />
  </Step>
  <Step stepNumber={3}>
    <h6>Update the import paths according to your project setup.</h6>
  </Step>
</Stepper>


## Usage

<CodeViewer
  className="mt-3"
  code={`import {
    Radio,
    RadioGroup
} from "@/components/ui/radio-group";`}
/>
<CodeViewer
  className="mt-3"
  code={`<RadioGroup defaultValue={"defult"}>
    <label className="flex items-center gap-2">
      <Radio value={"default"} />
      <span className="text-sm font-medium">Default</span>
    </label>
    <label className="flex items-center gap-2">
      <Radio value={"comfortable"} />
      <span className="text-sm font-medium">Comfortable</span>
    </label>
  </RadioGroup>`}
/>

