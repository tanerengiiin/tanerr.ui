import ComponentViewer from "@/components/docs/ComponentViewer";
import { Step, Stepper } from "@/components/docs/Stepper";
import CodeViewer from "@/components/docs/CodeViewer";
import NumberFieldDemo from "@/components/demo/number-field/NumberFieldDemo";

export const metadata = {
  title: "Number Field",
  breadcrumb: ["Docs", "Components", "Number Field"],
};

<ComponentViewer json="/data/components/demo/number-field/NumberFieldDemo.json">
  <NumberFieldDemo />
</ComponentViewer>

## Installation

<Stepper totalSteps={3}>
  <Step stepNumber={1}>
    <h6>Create `number-field.tsx` file in your `components/ui/` directory.</h6>
  </Step>
  <Step stepNumber={2}>
    <h6>Copy and paste the following code into the file.</h6>
    <CodeViewer className="mt-3" path="./src/components/ui/number-field.tsx" />
  </Step>
  <Step stepNumber={3}>
    <h6>Update the import paths according to your project setup.</h6>
  </Step>
</Stepper>

## Usage

<CodeViewer
  className="mt-3"
  code={`import {
    NumberField, 
    NumberFieldInput, 
    NumberFieldScrubArea
  } from "@/components/ui/number-field";`}
/>
<CodeViewer className="mt-3" code={`<NumberField>
      <NumberFieldScrubArea>
          <label className="font-medium text-sm">Amount</label>
      </NumberFieldScrubArea>
      <NumberFieldInput className="w-40"/>
  </NumberField>`} />
